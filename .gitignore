ig_pipeline/config.env
__pycache__/
*.pyc
content/story_atoms.json
.DS_Store
node_modules/

# 거래처 액세스 코드 — 절대 커밋 금지(리포가 공개다)
workers/.partner-codes.json

# 로컬 자격증명 — 절대 커밋 금지
.env
.env.local
.env.*.local
*.pem
*.key

# 도식 영상은 R2에 둔다 — 리포에 매일 1MB씩 영구히 쌓이는 걸 막는다
chart/assets/**/*.mp4
.wrangler/

# ★★ 성역 (KD 2026-08-26) — 내부 정보는 Public 에 절대 안 올린다.
# soonsal-internal(Private)로 이관됨. 여기 다시 추가되지 않게 막는다.
/scripts/
/ci/
/ig_pipeline/
/workers/
/_queue/
/_publish/
/_incoming/
/_drafts/
/ops/
/docs/
/content/
/.githooks/
/.wrangler/
/deploy.py
/deploy.sh
/CLAUDE.md
